{
  "name": "@askturret/grid-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run build:demo",
    "build:demo": "npm run build:grid && cd ../demo && npm install --include=dev && npx vite build && ls -la dist/ && mkdir -p ../landing/dist/demo && cp -r dist/* ../landing/dist/demo/ && ls -la ../landing/dist/demo/",
    "build:grid": "cd .. && npm install && npm run build",
    "preview": "astro preview",
    "start": "npx serve dist -s -l $PORT"
  },
  "dependencies": {
    "@astrojs/react": "^4.4.2",
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.16.6",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "serve": "^14.2.4",
    "tailwindcss": "^3.4.19"
  }
}
