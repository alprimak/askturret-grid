# Railway config for grid-landing service
# Keep root directory at repo root (not landing/)

[build]
buildCommand = "cd landing && npm install && npm run build"

[deploy]
startCommand = "cd landing && npx serve dist -l $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
