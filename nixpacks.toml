# Force Node.js 20 for Astro compatibility
[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

# Override the default build to use our custom script
[phases.build]
cmds = ["npm run build:railway"]

[start]
cmd = "npm run start"
